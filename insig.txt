const { Router } = require('express');
const router = Router();

const {
        deleteBranchSupply,
} = require('../../../controllers/safety/branch/branch-supply.controller');

const {
  validateJWT
} = require('../../../middlewares/validate-jwt');

router.delete('/:id', [validateJWT], deleteBranchSupply);

module.exports = router;
