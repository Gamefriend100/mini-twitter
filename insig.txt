import { Card } from "../../../../components/shared";
import { UseRequestContext } from "../../../../context/safety/kit/request/RequestContext";

const RejectionDetails = () => {

  const { selectedRequest } = UseRequestContext();

  return (
    <Card>
      <div className="space-y-4 text-sm">
        <div>
          <div className="flex flex-col items-center gap-8 text-xl">
            <h3 className="font-semibold text-black uppercase">Detalles del rechazo</h3>
            <hr className="w-full" />
          </div>
          <div className="flex flex-col gap-2">
            <h3 className="font-semibold text-gray-700 uppercase">Motivo de rechazo:</h3>
            <hr className="w-full" />
          </div>
          <div>{selectedRequest?.rejection?.name}</div>
        </div>
        <div className="space-y-6 ">
          <div className="flex flex-col gap-2">
            <h3 className="font-semibold text-gray-700 uppercase">Descripción:</h3>
            <hr className="w-full" />
          </div>
          <div>{selectedRequest?.rejectionReason}</div>
        </div>
        <div className="flex justify-end">
        </div>
      </div>
    </Card>
  );

};

export default RejectionDetails;
