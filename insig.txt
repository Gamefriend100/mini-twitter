import { Card } from "../../../../components/shared";
import { UseRequestContext } from "../../../../context/safety/kit/request/RequestContext";

const RejectionDetails = () => {
  const { selectedRequest } = UseRequestContext();

  return (
    <Card>
      <div className="p-6 space-y-6">

        <div className="flex flex-col gap-2 border-b pb-3">
          <h2 className="text-xl font-bold text-gray-800 uppercase tracking-wide">
            Detalles del rechazo
          </h2>
          <p className="text-sm text-gray-500">
            Información sobre la solicitud rechazada
          </p>
        </div>

        <div className="bg-gray-50 p-4 rounded-xl border">
          <h3 className="text-sm font-semibold text-gray-600 uppercase mb-2">
            Motivo de rechazo
          </h3>

          <p className="text-base text-gray-800 font-medium">
            {selectedRequest?.rejection?.name || "Sin motivo registrado"}
          </p>
        </div>

        <div className="bg-gray-50 p-4 rounded-xl border">
          <h3 className="text-sm font-semibold text-gray-600 uppercase mb-2">
            Descripción
          </h3>

          <p className="text-gray-700 leading-relaxed">
            {selectedRequest?.rejectionReason || "Sin descripción"}
          </p>
        </div>

      </div>
    </Card>
  );
};

export default RejectionDetails;
